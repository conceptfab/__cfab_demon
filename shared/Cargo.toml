[package]
name = "timeflow-shared"
version = "0.1.0"
edition = "2021"

[lib]
path = "lib.rs"

[dependencies]
log = "0.4"
rusqlite = { version = "0.31", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
